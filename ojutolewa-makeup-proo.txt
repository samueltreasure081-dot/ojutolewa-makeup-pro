<!DOCTYPE html>
<html lang="en">
<head>
<meta name="google-site-verification" content="A-Lr0CL7xxF19gkCTI-4oXhrsHmuGsOhI1bgjC63gcA" />
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>OJUTOLEWA-MAKEUP Pro | Professional Makeup Services</title>
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@700&family=Lato:wght@400;700&display=swap" rel="stylesheet">

  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      background: linear-gradient(to right, #000000, #2f2f2f);
      color: white;
      line-height: 1.8;
      padding: 40px 20px;
      font-family: 'Lato', sans-serif;
      scroll-behavior: smooth;
    }

    /* Navigation */
    nav {
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      background: rgba(0, 0, 0, 0.95);
      padding: 15px 40px;
      z-index: 1000;
      display: flex;
      justify-content: space-between;
      align-items: center;
      box-shadow: 0 2px 10px rgba(255, 215, 0, 0.2);
      transform: translateY(-100%);
      transition: transform 0.3s ease;
    }

    nav.active {
      transform: translateY(0);
    }

    nav .logo {
      color: #ffd700;
      font-size: 20px;
      font-weight: bold;
      font-family: 'Playfair Display', serif;
    }

    nav ul {
      display: flex;
      list-style: none;
      gap: 30px;
    }

    nav a {
      color: white;
      text-decoration: none;
      transition: color 0.3s ease;
    }

    nav a:hover {
      color: #ffd700;
    }

    .menu-toggle {
      display: none;
      background: none;
      border: none;
      color: #ffd700;
      font-size: 24px;
      cursor: pointer;
    }

    body {
      padding-top: 60px;
    }

    h1 {
      color: #ffd700;
      margin-bottom: 20px;
      font-size: 42px;
      font-family: 'Playfair Display', serif;
      animation: fadeInDown 0.8s ease-out;
    }

    h3 {
      color: #ffd700;
      font-size: 24px;
      margin-bottom: 15px;
      font-family: 'Playfair Display', serif;
    }

    p {
      margin-bottom: 20px;
      font-size: 16px;
      animation: fadeInUp 0.8s ease-out;
    }

    blockquote {
      background: #1f1f1f;
      padding: 20px;
      border-radius: 15px;
      margin: 25px 0;
      border-left: 4px solid #ffd700;
      font-style: italic;
    }

    ul {
      padding-left: 20px;
      margin-bottom: 20px;
    }

    li {
      margin-bottom: 8px;
    }

    /* Services Section */
    .services {
      margin: 50px 0;
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 20px;
    }

    .service-card {
      background: #1f1f1f;
      padding: 20px;
      border-radius: 15px;
      border-left: 4px solid #ffd700;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
      cursor: pointer;
    }

    .service-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 8px 20px rgba(255, 215, 0, 0.3);
    }

    .service-card h4 {
      color: #ffd700;
      margin-bottom: 10px;
    }

    /* Button Styling */
    .btn {
      display: inline-block;
      background: #ffd700;
      color: #000000;
      padding: 12px 30px;
      border-radius: 8px;
      text-decoration: none;
      font-weight: bold;
      margin: 15px 0;
      transition: all 0.3s ease;
      border: 2px solid #ffd700;
      cursor: pointer;
    }

    .btn:hover {
      background: transparent;
      color: #ffd700;
      transform: scale(1.05);
    }

    /* Link Styling */
    a {
      color: #ffd700;
    }

    a:hover {
      text-decoration: underline;
    }

    /* Footer */
    footer {
      margin-top: 50px;
      text-align: center;
      background: #1f1f1f;
      color: #ffffff;
      padding: 30px;
      border-radius: 20px;
      border-top: 4px solid #ffd700;
      box-shadow: 0 4px 15px rgba(0, 0, 0, 0.3);
    }

    footer p {
      margin-bottom: 15px;
    }

    /* Modal */
    .modal {
      display: none;
      position: fixed;
      z-index: 1001;
      left: 0;
      top: 0;
      width: 100%;
      height: 100%;
      background-color: rgba(0, 0, 0, 0.8);
      animation: fadeIn 0.3s ease;
    }

    .modal.show {
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .modal-content {
      background: #1f1f1f;
      padding: 30px;
      border-radius: 15px;
      width: 90%;
      max-width: 500px;
      border: 2px solid #ffd700;
      animation: slideIn 0.3s ease;
    }

    .close-btn {
      color: #ffd700;
      float: right;
      font-size: 28px;
      font-weight: bold;
      cursor: pointer;
      transition: color 0.3s ease;
    }

    .close-btn:hover {
      color: #ffffff;
    }

    /* Form Styling */
    .form-group {
      margin-bottom: 20px;
      text-align: left;
    }

    label {
      display: block;
      margin-bottom: 5px;
      color: #ffd700;
      font-weight: bold;
    }

    input,
    textarea,
    select {
      width: 100%;
      padding: 10px;
      border: 1px solid #ffd700;
      border-radius: 5px;
      background: #2f2f2f;
      color: white;
      font-family: 'Lato', sans-serif;
      font-size: 14px;
    }

    input::placeholder,
    textarea::placeholder {
      color: #999;
    }

    input:focus,
    textarea:focus,
    select:focus {
      outline: none;
      box-shadow: 0 0 10px rgba(255, 215, 0, 0.5);
    }

    textarea {
      resize: vertical;
      min-height: 100px;
    }

    /* Animations */
    @keyframes fadeIn {
      from {
        opacity: 0;
      }
      to {
        opacity: 1;
      }
    }

    @keyframes fadeInDown {
      from {
        opacity: 0;
        transform: translateY(-20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    @keyframes fadeInUp {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    @keyframes slideIn {
      from {
        transform: translateY(-50px);
        opacity: 0;
      }
      to {
        transform: translateY(0);
        opacity: 1;
      }
    }

    /* Scroll to Top Button */
    #scrollTopBtn {
      display: none;
      position: fixed;
      bottom: 30px;
      right: 30px;
      background: #ffd700;
      color: #000;
      border: none;
      padding: 10px 15px;
      border-radius: 50%;
      cursor: pointer;
      font-size: 18px;
      z-index: 999;
      transition: all 0.3s ease;
    }

    #scrollTopBtn:hover {
      background: #fff;
      transform: scale(1.1);
    }

    #scrollTopBtn.show {
      display: block;
    }

    /* Responsive Design */
    @media (max-width: 768px) {
      body {
        padding: 20px 15px;
        padding-top: 60px;
      }

      h1 {
        font-size: 32px;
      }

      p {
        font-size: 14px;
      }

      nav ul {
        display: none;
      }

      nav.active ul {
        display: flex;
        flex-direction: column;
        position: absolute;
        top: 60px;
        left: 0;
        right: 0;
        background: rgba(0, 0, 0, 0.95);
        padding: 20px;
        gap: 15px;
      }

      .menu-toggle {
        display: block;
      }

      .services {
        grid-template-columns: 1fr;
      }
    }

    @media (max-width: 480px) {
      body {
        padding: 15px 10px;
      }

      h1 {
        font-size: 24px;
      }

      .btn {
        display: block;
        width: 100%;
        text-align: center;
      }

      nav {
        padding: 15px 20px;
      }

      #scrollTopBtn {
        bottom: 20px;
        right: 20px;
        padding: 8px 12px;
        font-size: 16px;
      }
    }
  </style>
</head>https://treashprogramm16-web.github.io/ojutolewa-makeup-pro/

<body>
  <!-- Navigation -->
  <nav id="navbar">
    <div class="logo">OJUTOLEWA-MAKEUP</div>
    <button class="menu-toggle" id="menuToggle">☰</button>
    <ul id="navMenu">
      <li><a href="#home">Home</a></li>
      <li><a href="#services">Services</a></li>
      <li><a href="#about">About</a></li>
      <li><a href="#contact">Contact</a></li>
    </ul>
  </nav>

  <!-- Main Content -->
  <section id="home">
    <h1>OJUTOLEWA-MAKEUP PRO</h1>

    <p>
      <cite>
        "Makeup is not a mask that covers up your beauty; it's a weapon that helps you express who you are from the inside."
        — Michelle Phan
      </cite>
    </p>
  </section>

  <section id="about">
    <p>
      OJUTOLEWA-MAKEUP Pro is a creative and professional makeup brand by
      <strong>Victoria Okonofua</strong> dedicated to enhancing beauty,
      confidence, and elegance. The company specializes in modern makeup
      artistry for different occasions such as weddings, birthdays,
      fashion events, photoshoots, and special celebrations.
    </p>

    <p>
      At OJUTOLEWA-MAKEUP Pro, beauty is seen as an expression of
      personality and style. The brand focuses on delivering high-quality
      makeup services with precision, creativity, and professionalism.
      Using quality products and modern techniques, the company aims to
      give every client a flawless and long-lasting look.
    </p>

    <blockquote>
      <p><strong>OJUTOLEWA-MAKEUP Pro is built on:</strong></p>
      <ul>
        <li>Values of excellence</li>
        <li>Customer satisfaction</li>
        <li>Creativity</li>
        <li>Innovation in the beauty industry</li>
      </ul>
    </blockquote>

    <p>
      The enterprise continues to grow by creating unique styles that
      match individual preferences and current fashion trends.
    </p>
  </section>

  <!-- Services Section -->
  <section id="services">
    <h2 style="color: #ffd700; font-size: 32px; margin-bottom: 30px;">Our Services</h2>
    <div class="services">
      <div class="service-card" onclick="openModal('Wedding Makeup')">
        <h4>💍 Wedding Makeup</h4>
        <p>Perfect bridal and special occasion makeup that lasts all day.</p>
      </div>
      <div class="service-card" onclick="openModal('Birthday Makeup')">
        <h4>🎂 Birthday Makeup</h4>
        <p>Fun and creative makeup for your birthday celebrations.</p>
      </div>
      <div class="service-card" onclick="openModal('Photoshoot Makeup')">
        <h4>📸 Photoshoot Makeup</h4>
        <p>Professional makeup specially designed for camera appearances.</p>
      </div>
      <div class="service-card" onclick="openModal('Event Makeup')">
        <h4>✨ Event Makeup</h4>
        <p>Glamorous makeup for parties, galas, and special events.</p>
      </div>
    </div>
  </section>

  <!-- Contact Section -->
  <section id="contact">
    <footer>
      <h3>OJUTOLEWA-MAKEUP</h3>

      <p>
        <cite>Enhancing Beauty, Confidence & Elegance</cite>
      </p>

      <button class="btn" onclick="openModal('Book Appointment')">
        Book Appointment
      </button>

      <p style="margin-top: 30px;">
        Professional Makeup Services for Weddings, Birthdays,
        Photoshoots, Special Events.
      </p>

      <p>
        📍 Ogun State, Sagamu, Nigeria |
        📞 +2347032747439
      </p>

      <p>
        © 2026 OJUTOLEWA-MAKEUP Pro. All Rights Reserved.
      </p>
    </footer>
  </section>

  <!-- Modal for Booking/Details -->
  <div id="appointmentModal" class="modal">
    <div class="modal-content">
      <span class="close-btn" onclick="closeModal()">&times;</span>
      <h3 id="modalTitle">Book Appointment</h3>

      <form id="appointmentForm">
        <div class="form-group">
          <label for="name">Full Name *</label>
          <input type="text" id="name" name="name" required placeholder="Enter your full name">
        </div>

        <div class="form-group">
          <label for="email">Email *</label>
          <input type="email" id="email" name="email" required placeholder="Enter your email">
        </div>

        <div class="form-group">
          <label for="phone">Phone Number *</label>
          <input type="tel" id="phone" name="phone" required placeholder="Enter your phone number">
        </div>

        <div class="form-group">
          <label for="service">Service *</label>
          <select id="service" name="service" required>
            <option value="">Select a service</option>
            <option value="Wedding Makeup">Wedding Makeup</option>
            <option value="Birthday Makeup">Birthday Makeup</option>
            <option value="Photoshoot Makeup">Photoshoot Makeup</option>
            <option value="Event Makeup">Event Makeup</option>
          </select>
        </div>

        <div class="form-group">
          <label for="date">Preferred Date *</label>
          <input type="date" id="date" name="date" required>
        </div>

        <div class="form-group">
          <label for="message">Message/Special Requests</label>
          <textarea id="message" name="message" placeholder="Tell us about your makeup preferences..."></textarea>
        </div>

        <button type="submit" class="btn" style="width: 100%; text-align: center;">Submit Appointment</button>
      </form>
    </div>
  </div>

  <!-- Scroll to Top Button -->
  <button id="scrollTopBtn" onclick="scrollToTop()">⬆</button>

  <!-- JavaScript -->
  <script>
    // Mobile Menu Toggle
    const menuToggle = document.getElementById('menuToggle');
    const navMenu = document.getElementById('navMenu');
    const navbar = document.getElementById('navbar');

    menuToggle.addEventListener('click', () => {
      navbar.classList.toggle('active');
    });

    // Close menu when clicking on a link
    document.querySelectorAll('#navMenu a').forEach(link => {
      link.addEventListener('click', () => {
        navbar.classList.remove('active');
      });
    });

    // Show navbar on scroll
    let lastScrollTop = 0;
    window.addEventListener('scroll', () => {
      let currentScroll = window.pageYOffset;
      if (currentScroll > lastScrollTop) {
        navbar.classList.remove('active');
      }
      lastScrollTop = currentScroll <= 0 ? 0 : currentScroll;
    });

    // Modal Functions
    const modal = document.getElementById('appointmentModal');
    const modalTitle = document.getElementById('modalTitle');
    const appointmentForm = document.getElementById('appointmentForm');

    function openModal(service = 'Book Appointment') {
      modal.classList.add('show');
      modalTitle.textContent = service;
      if (service !== 'Book Appointment') {
        document.getElementById('service').value = service;
      }
    }

    function closeModal() {
      modal.classList.remove('show');
      appointmentForm.reset();
    }

    // Close modal when clicking outside
    modal.addEventListener('click', (e) => {
      if (e.target === modal) {
        closeModal();
      }
    });

    // Form Submission
    appointmentForm.addEventListener('submit', (e) => {
      e.preventDefault();

      const formData = new FormData(appointmentForm);
      const name = formData.get('name');
      const email = formData.get('email');
      const phone = formData.get('phone');
      const service = formData.get('service');
      const date = formData.get('date');
      const message = formData.get('message');

      // Validate form
      if (!name || !email || !phone || !service || !date) {
        alert('Please fill in all required fields!');
        return;
      }

      // Create WhatsApp message
      const whatsappMessage = `Hello OJUTOLEWA-MAKEUP Pro,\n\nI would like to book an appointment:\n\nName: ${name}\nEmail: ${email}\nPhone: ${phone}\nService: ${service}\nDate: ${date}\nMessage: ${message}`;

      const whatsappLink = `https://wa.me/2347032747439?text=${encodeURIComponent(whatsappMessage)}`;

      // Show success message
      alert('Thank you! Redirecting to WhatsApp...');

      // Open WhatsApp
      window.open(whatsappLink, '_blank');

      // Close modal and reset form
      closeModal();
    });

    // Scroll to Top Button
    const scrollTopBtn = document.getElementById('scrollTopBtn');

    window.addEventListener('scroll', () => {
      if (window.pageYOffset > 300) {
        scrollTopBtn.classList.add('show');
      } else {
        scrollTopBtn.classList.remove('show');
      }
    });

    function scrollToTop() {
      window.scrollTo({
        top: 0,
        behavior: 'smooth'
      });
    }

    // Set minimum date to today
    const dateInput = document.getElementById('date');
    const today = new Date().toISOString().split('T')[0];
    dateInput.setAttribute('min', today);

    // Console greeting
    console.log('Welcome to OJUTOLEWA-MAKEUP Pro! 💄');
    console.log('Enhancing Beauty, Confidence & Elegance');
  </script>
</body>
</html>
